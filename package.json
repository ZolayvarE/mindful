{
  "name": "mindful",
  "version": "1.2.2",
  "description": "A user friendly state manager for React.",
  "main": "index.js",
  "scripts": {
    "patch": "npm run commit && npm version patch && npm publish",
    "commit": "(git add . || true) && (git commit || true) && (git push origin master || true)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "React",
    "Redux",
    "state",
    "manager",
    "state=manager",
    "management",
    "state-management"
  ],
  "author": "Eric Zolayvar",
  "license": "MIT"
}
